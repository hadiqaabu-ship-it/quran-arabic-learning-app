# 贡献指南

感谢参与改进。

## 提交前

1. 一个提交只处理一个清晰问题。
2. 不要提交音频、教材、PDF、APK、签名密钥、测试录音或设备日志。
3. 新增数据必须说明来源、版本、许可证、是否允许修改和再分发。
4. 用户界面只保留学习者需要的信息，不展示内部审核、生成模型或开发流程提示。
5. 当前无音频发布阶段不得提交任何音频文件或非空音频映射。
6. 若未来恢复音频，经文界面中的文本与音频范围必须一致；若播放完整经文，界面也必须显示完整经文。

## 基本检查

提交前至少确认：

- `web/app.js` 通过 JavaScript 语法检查；
- `node scripts/validate-community-content.mjs` 通过；
- 页面无控制台错误；
- 所有可点击入口都有结果或清晰的不可用说明；
- 不含大于 95 MB 的文件；
- 不含密钥、令牌和个人数据。

## 版权声明

提交代码即表示你有权按照 GPL-3.0-only 提供该代码。第三方内容不因进入本项目而自动变成 GPL 内容。
